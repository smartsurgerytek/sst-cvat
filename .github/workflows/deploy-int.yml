# =============================================================================
# PROD Workflow: Build and Deploy to AWS Production Environment
# =============================================================================
#
# This workflow builds CVAT images and deploys them to AWS Production.
# Triggered manually only (no auto-deploy to production).
#
# Usage:
#   1. Go to Actions tab in GitHub
#   2. Select "Deploy to PROD" workflow
#   3. Click "Run workflow"
#   4. Select your branch from dropdown
#   5. Click "Run workflow" button
#
# Prerequisites (GitHub Secrets):
#   - AWS_ACCESS_KEY_ID:     AWS access key
#   - AWS_SECRET_ACCESS_KEY: AWS secret key
#   - AWS_ACCOUNT_ID:        AWS account ID (587128718062)
#   - PROD_EC2_HOST:         PROD EC2 domain (cvat.getsmartsurgery.net or 3.224.187.219)
#   - PROD_SSH_KEY:          SSH private key for PROD EC2 access
#
# =============================================================================

name: Deploy to PROD AWS

on:
  # Manual trigger only (production should never auto-deploy)
  workflow_dispatch:
    inputs:
      skip_backup:
        description: 'Skip backup before deployment'
        required: false
        type: boolean
        default: false

# Prevent duplicate runs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  AWS_REGION: us-east-1
  DEPLOY_PATH: /home/ubuntu/cvat/sst-cvat

jobs:
  # ============================================
  # Job 1: Prepare Build Parameters
  # ============================================
  prepare:
    name: Prepare Build Parameters
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.vars.outputs.image_tag }}
    steps:
      - name: Generate image tag with short SHA
        id: vars
        run: |
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          BRANCH_TAG=$(echo "${{ github.ref_name }}" | sed 's/\//-/g')
          IMAGE_TAG="${BRANCH_TAG}-${SHORT_SHA}"
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT

  # ============================================
  # Job 2: Build Images (reusable workflow)
  # ============================================
  build:
    name: Build Images
    needs: prepare
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR (US East 1)
        id: login-ecr
        run: |
          aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
            docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com

      - name: Build Info
        run: |
          echo "============================================"
          echo "Building CVAT Images for PROD"
          echo "============================================"
          echo "Branch:       ${{ github.ref_name }}"
          echo "Image Tag:    ${{ needs.prepare.outputs.image_tag }}"
          echo "ECR Registry: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"
          echo "Region:       ${{ env.AWS_REGION }}"
          echo "============================================"

      - name: Build and push CVAT Server
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/cvat-server:${{ needs.prepare.outputs.image_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            CVAT_VERSION=${{ github.sha }}

      - name: Build and push CVAT UI
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.ui
          push: true
          tags: |
            ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/cvat-ui:${{ needs.prepare.outputs.image_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            CVAT_ENV_LABEL=PROD

  # ============================================
  # Job 3: Deploy to AWS Production
  # ============================================
  deploy:
    name: Deploy to PROD Environment
    runs-on: ubuntu-latest
    needs: [prepare, build]

    steps:
      - name: Set deployment variables
        id: vars
        run: |
          echo "image_tag=${{ needs.prepare.outputs.image_tag }}" >> $GITHUB_OUTPUT
          echo "branch_name=${{ github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Deployment Info
        run: |
          echo "============================================"
          echo "Deploying to AWS PROD Environment"
          echo "============================================"
          echo "Branch:       ${{ steps.vars.outputs.branch_name }}"
          echo "Image Tag:    ${{ steps.vars.outputs.image_tag }}"
          echo "Skip Backup:  ${{ github.event.inputs.skip_backup }}"
          echo "Triggered by: ${{ github.actor }}"
          echo "Region:       ${{ env.AWS_REGION }}"
          echo "============================================"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          # Use printf to preserve multiline SSH key correctly
          printf "%s" "${{ secrets.PROD_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          # Add host key (non-fatal if it fails)
          ssh-keyscan -H "${{ secrets.PROD_EC2_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Prepare deployment files
        id: prepare
        run: |
          IMAGE_TAG="${{ steps.vars.outputs.image_tag }}"
          ECR_REGISTRY="${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"
          CVAT_HOST="cvat.getsmartsurgery.net"

          # Create .env file
          cat > .env << EOF
          # CVAT Environment Configuration
          # Generated by GitHub Actions deployment
          CVAT_HOST=${CVAT_HOST}
          CVAT_SERVER_IMAGE=${ECR_REGISTRY}/cvat-server:${IMAGE_TAG}
          CVAT_UI_IMAGE=${ECR_REGISTRY}/cvat-ui:${IMAGE_TAG}
          ACME_EMAIL=krishtopher.rathod@faciletechnolab.com
          EOF

          echo "Created .env file:"
          cat .env

      - name: Copy deployment files to EC2
        run: |
          # Copy docker-compose files and .env to EC2
          scp -i ~/.ssh/id_rsa \
            docker-compose.yml \
            docker-compose.stg.yml \
            docker-compose.https.yml \
            .env \
            ubuntu@${{ secrets.PROD_EC2_HOST }}:${{ env.DEPLOY_PATH }}/

          # Copy components directory (contains config files mounted by docker-compose)
          scp -r -i ~/.ssh/id_rsa \
            components \
            ubuntu@${{ secrets.PROD_EC2_HOST }}:${{ env.DEPLOY_PATH }}/

          echo "Deployment files copied to EC2:${{ env.DEPLOY_PATH }}/"

      - name: Deploy to PROD EC2
        run: |
          IMAGE_TAG="${{ steps.vars.outputs.image_tag }}"
          BRANCH_NAME="${{ steps.vars.outputs.branch_name }}"
          DEPLOY_PATH="${{ env.DEPLOY_PATH }}"
          SKIP_BACKUP="${{ github.event.inputs.skip_backup }}"
          ECR_REGISTRY="${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"

          ssh -i ~/.ssh/id_rsa ubuntu@${{ secrets.PROD_EC2_HOST }} << ENDSSH
            set -e

            echo '============================================'
            echo 'Starting deployment on AWS PROD EC2'
            echo '============================================'

            # Configure AWS CLI for ECR login
            export AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}"
            export AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}"
            export AWS_DEFAULT_REGION="${{ env.AWS_REGION }}"

            # Login to ECR (US East 1)
            echo 'Logging into Amazon ECR (US East 1)...'
            aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
              docker login --username AWS --password-stdin ${ECR_REGISTRY}

            cd ${DEPLOY_PATH}

            echo 'Deployment directory: ${DEPLOY_PATH}'
            echo ''
            echo 'Files in deployment directory:'
            ls -la

            # Create backup (unless skipped)
            if [ '${SKIP_BACKUP}' != 'true' ]; then
              echo ''
              echo 'Creating backup of current state...'
              BACKUP_TAG=\$(date +%Y%m%d_%H%M%S)
              docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}' > /tmp/cvat_backup_\${BACKUP_TAG}.txt 2>/dev/null || true
              echo "Backup saved to /tmp/cvat_backup_\${BACKUP_TAG}.txt"
            fi

            echo ''
            echo '.env file contents:'
            cat .env

            # Source .env to get image names
            export \$(cat .env | grep -v '^#' | xargs)

            echo ''
            echo 'Pulling new images...'
            echo "  Server: \${CVAT_SERVER_IMAGE}"
            echo "  UI:     \${CVAT_UI_IMAGE}"

            docker pull \${CVAT_SERVER_IMAGE}
            docker pull \${CVAT_UI_IMAGE}

            echo 'Stopping current containers...'
            docker compose \
              -f docker-compose.yml \
              -f docker-compose.stg.yml \
              -f docker-compose.https.yml \
              -f components/serverless/docker-compose.serverless.yml \
              down --remove-orphans || true

            echo 'Starting containers with new images (with HTTPS and serverless support)...'
            docker compose \
              -f docker-compose.yml \
              -f docker-compose.stg.yml \
              -f docker-compose.https.yml \
              -f components/serverless/docker-compose.serverless.yml \
              up -d

            echo 'Waiting for services to start...'
            sleep 15

            echo 'Checking container status...'
            docker compose \
              -f docker-compose.yml \
              -f docker-compose.stg.yml \
              -f docker-compose.https.yml \
              -f components/serverless/docker-compose.serverless.yml \
              ps

            echo '============================================'
            echo 'Deployment complete!'
            echo 'Branch:    ${BRANCH_NAME}'
            echo 'Image tag: ${IMAGE_TAG}'
            echo '============================================'
          ENDSSH

      - name: Health Check
        run: |
          echo "Running health check..."
          sleep 20

          ssh -i ~/.ssh/id_rsa ubuntu@${{ secrets.PROD_EC2_HOST }} << 'ENDSSH'
            cd ${{ env.DEPLOY_PATH }}

            echo 'Container Status:'
            docker compose \
              -f docker-compose.yml \
              -f docker-compose.stg.yml \
              -f docker-compose.https.yml \
              -f components/serverless/docker-compose.serverless.yml \
              ps

            FAILED=0

            for SERVICE in cvat_server cvat_ui cvat_db nuclio traefik; do
              STATUS=$(docker compose \
                -f docker-compose.yml \
                -f docker-compose.stg.yml \
                -f docker-compose.https.yml \
                -f components/serverless/docker-compose.serverless.yml \
                ps ${SERVICE} --format '{{.Status}}' 2>/dev/null || echo 'not found')

              if [[ "${STATUS}" == *'Up'* ]]; then
                echo "✓ ${SERVICE}: Running"
              else
                echo "✗ ${SERVICE}: ${STATUS}"
                FAILED=1
              fi
            done

            if [ ${FAILED} -eq 1 ]; then
              echo ''
              echo 'Some services failed to start. Check logs with:'
              echo '  docker compose logs <service_name>'
              exit 1
            fi

            echo ''
            echo 'All critical services are running!'
          ENDSSH

      - name: Cleanup SSH keys
        if: always()
        run: |
          rm -f ~/.ssh/id_rsa

      - name: Deployment Summary
        if: always()
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | **PROD (AWS)** |" >> $GITHUB_STEP_SUMMARY
          echo "| Branch | \`${{ steps.vars.outputs.branch_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Image Tag | \`${{ steps.vars.outputs.image_tag }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| EC2 Host | \`${{ secrets.PROD_EC2_HOST }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| CVAT URL | https://cvat.getsmartsurgery.net |" >> $GITHUB_STEP_SUMMARY
          echo "| Triggered By | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Status | ${{ job.status }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "1. Test on PROD environment: https://cvat.getsmartsurgery.net" >> $GITHUB_STEP_SUMMARY
          echo "2. Monitor for any issues" >> $GITHUB_STEP_SUMMARY